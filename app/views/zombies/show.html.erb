<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @zombie.name %>
</p>

<p>
  <strong>Bio:</strong>
  <%= @zombie.bio %>
</p>

<%# level 3 example code%>
<% brain = Brain.find(1) %>
<h2><%= brain.flavor %></h2>

<% zo = @zombie.id %>
<p>The brain: <%= Brain.where(zombie_id: zo).first.status %></p>
<p>Go to all <%= link_to "Zombies", zombies_path,  data: { confirm: 'Are you sure?'} %></p>

<%# level 3 example code%>


<%= link_to 'Edit', edit_zombie_path(@zombie) %>
<%= link_to 'Back', zombies_path %>
